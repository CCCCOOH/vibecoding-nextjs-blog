## 目标：
    1. 删除Posts per Year的chart
    2. 新增功能支持将项目打包成静态文件一键部署到vercel上
    3. 新增目录管理功能，允许像文件夹一样将文章分类到目录中，并且在后台中可调整和编辑目录
    4. config界面保存后弹出悬浮的成功或失败提示。
    5. 增加footer并显示config中的页脚信息
## 输入
    1. 当前项目的代码以及doc/下的项目信息
## 输出
    1. 实现我的目标并将更新日志写入到/doc/log.nd
    2. 根据功能更新USAGE.md（使用文档）
## 步骤
    - 请使用提问的方式帮助我确认需求
    - 不要猜测我的意图，任何不明确的地方都必须向我提问